  · "POST /api/packages HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools HTTP/1.1" 200 -
  · "GET /api/schema/drop_pools HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/weighted_a HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/fixed_a HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/table_a HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/choice_a HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/pool_a HTTP/1.1" 200 -
  · "PUT /api/package/loot_demo/d/drop_pools/pool_b HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools/weighted_a/preview HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools/fixed_a/preview HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools/table_a/preview HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools/choice_a/preview HTTP/1.1" 200 -
  · "GET /api/package/loot_demo/d/drop_pools/ghost/preview HTTP/1.1" 422 -
  · "GET /api/package/loot_demo/d/maps/nope/preview HTTP/1.1" 404 -
  · "GET /api/package/loot_demo/d/nosuchdom/x/preview HTTP/1.1" 404 -
  · "GET /api/package/nope/d/drop_pools/weighted_a/preview HTTP/1.1" 404 -
  · "PUT /api/package/loot_demo/d/drop_pools/bad_one HTTP/1.1" 422 -
  · "PUT /api/package/loot_demo/d/drop_pools/bad_two HTTP/1.1" 422 -
  · "GET /api/package/loot_demo/d/drop_pools HTTP/1.1" 200 -
  · "GET /api/domains HTTP/1.1" 200 -
  · "GET /api/glossary HTTP/1.1" 200 -
  · "GET /api/package/loot_demo HTTP/1.1" 200 -
== drop_pools 域（随机产出形状）编辑器门禁 ==

[1] 域注册与 schema
  ✅ drop_pools 域已注册
  ✅ label = 掉落池 / icon = 🎁 / kind = data
  ✅ schema 文件存在（drop_pools.schema.json）
  ✅ primary='pool' 在 schema $defs 里且 = x-primary
  ✅ $defs 至少含 pool / pool_entry / pool_roll
  ✅ schema 里零 enum（策略名/取值必须是自由串，枚举会是内容分类）
  ✅ pool.required 只有 type
  ✅ pool_entry 必填 item
  ✅ pool_entry 允许扩展字段（additionalProperties）
  ✅ pool_roll 必填 pool
  ✅ type 字段是字符串且不设枚举（内容侧可注册新策略）
  ✅ pool 有 2 个 examples
  ✅ examples 不含具体游戏词（只用 mat_a / pool_a 这类假名）
  ✅ 域 → schema 映射表里有 drop_pools（词典接口不会漏这个域）

[2] 字段词典与分组
  ✅ 词典有 drop_pools 域且非空
  ✅ schema 全部 14 个字段都有中文名
  ✅ 每条词典都有注脚（不留空壳）
  ✅ wiki 引用逐条可解析（页存在 + 词真在页里）
  ✅ 表单分组 4 组（≥3），且每组非空
  ✅ 分组无重复字段
  ✅ 全部字段一个不漏地分到组里
  ✅ 组里没有 schema 不存在的字段（防拼错）
  ✅ 分组 id 唯一且都有标签
  ✅ all_groups 覆盖 drop_pools（前端面板不空白）
  ✅ chance / cutoff 是 0~1 → 给滑杆

[3] 预览：四种内置策略的结构与权重
  ✅ weighted 预览成功
  ✅ type / strategy_uses 正确
  ✅ entries 按占比降序（最常见的在前）
  ✅ ★ share = 权重 ÷ 池内权重和（5/6 与 1/6）
  ✅ share_pct 给人看的百分数
  ✅ 带等级窗口的条目原样列出（预览不替它判断会不会进候选）
  ✅ expanded_count/unique 与引擎直算一致
  ✅ 审计通过（无断链）
  ✅ ★ 不装懂：一定有 resolver warning
  ✅ fixed 列出全部条目、保持声明序
  ✅ fixed 的 share / w 都是 null（占比不适用，不给假数）
  ✅ fixed 的 n 原样带回
  ✅ table_choice 预览成功且有 rolls 无 entries
  ✅ rolls 列出 cutoff（互斥档）
  ✅ rolls 列出 n 区间与 fallback
  ✅ 子池被识别（is_sub_pool）并递归展开
  ✅ cutoff 累计 = 1.0 → 不给「累计不足」警告
  ✅ ★ expanded_count = 引擎对子池递归展开的和（2 + 2）
  ✅ table 列出 chance 与子池展开
  ✅ table 的 cutoff 是 None（别把两种语义混起来）
  ✅ ★ 四个池的展开计数与引擎 LootTable 直算逐项一致
  ✅ 引擎审计：这批数据本身是干净的（issues 为空）

[4] 预览：坏数据 / 池不存在 → 明确原因，不假装有内容
  ✅ 坏数据被拦（没有这条池）→ ok=False
  ✅   └ 原因里出现「没有这条池」
  ✅ 坏数据被拦（不是对象）→ ok=False
  ✅   └ 原因里出现「不是对象」
  ✅ 坏数据被拦（缺少 type）→ ok=False
  ✅   └ 原因里出现「缺少 type」
  ✅ 坏数据被拦（必须是条目数组）→ ok=False
  ✅   └ 原因里出现「必须是条目数组」
  ✅ 坏数据被拦（缺 item）→ ok=False
  ✅   └ 原因里出现「缺 item」
  ✅ 坏数据被拦（权重和）→ ok=False
  ✅   └ 原因里出现「权重和」
  ✅ 坏数据被拦（缺 pool）→ ok=False
  ✅   └ 原因里出现「缺 pool」
  ✅ 坏数据被拦（必须是数组）→ ok=False
  ✅   └ 原因里出现「必须是数组」
  ✅ 整表不是对象也被拦
  ✅ 子池不存在 → 明确 warning（前缀/引用由内容侧注册，预览不猜）
  ✅ 子池不存在 → 引擎审计报「断链」（预览与引擎同判）
  ✅ 循环引用 → 报「坏数据」而不是崩（不是 500/RecursionError 外泄）
  ✅ 审计只报**本池**的问题（不把别的池的断链算到自己头上）
  ❌ 未注册的策略名 → 明确警告 + 按引擎兜底策略解析 ["策略名 'fish_x' 不在内置策略表里（内置：fixed, table, table_choice, weighted）——★ 运行期 `roll_pool` 会抛 `UnknownStrategy`（**不再静默回落 weighted**）。本预览按 weighted 展示结构，仅供看形状；要「每项按 chance 独立掷」请改用 `table`。", '引用解析需要内容侧提供 resolver —— 预览只显示结构与权重，不判断某个引用（如 mat_a）到底是什么东西、也不会替它编一个答案。', "策略名 'fish_x' 未注册 —— 内容侧可注册自己的策略；未注册时运行期会抛 `UnknownStrategy`，**不再按 weighted 兜底**。"]

[5] schema 校验：合法过、非法被拦
  ✅ weighted 合法条目通过校验
  ✅ fixed 合法条目通过校验
  ✅ table 合法条目通过校验
  ✅ table_choice 合法条目通过校验
  ✅ 缺 type 被拦
  ✅ 条目缺 item 被拦
  ✅ roll 缺 pool 被拦
  ✅ chance > 1 被拦
  ✅ 自由策略名能过校验（不设枚举的证据）

[6] HTTP 端到端：编辑器 → 数据文件 → 引擎
  ✅ 建包（只勾 drop_pools 域）成功
  ✅ 新建包的 drop_pools 域可列（空表）
  ✅ GET /api/schema/drop_pools 有 schema
  ✅ PUT weighted_a（weighted）落盘成功
  ✅ PUT fixed_a（fixed）落盘成功
  ✅ PUT table_a（table）落盘成功
  ✅ PUT choice_a（table_choice）落盘成功
  ✅ PUT pool_a（weighted）落盘成功
  ✅ PUT pool_b（fixed）落盘成功
  ✅ 列表 6 条
  ✅ GET preview（weighted）200
  ✅ ★ 端到端占比 = 权重 ÷ 权重和（5/6）
  ✅ ★ 端到端展开计数 = 引擎 expand（6 / 2）
  ✅ 端到端带 resolver warning + 审计通过
  ✅ preview（fixed）列出条目且 share=null
  ✅ preview（table）列出 chance + 子池展开
  ✅ preview（table_choice）列出 cutoff
  ✅ 不存在的池 → 422 + 中文原因
  ✅ 不存在的池 → 不假装有内容（无 entries/rolls）
  ✅ 预览只服务 drop_pools（别的域 → 404）
  ✅ 未知域 → 404
  ✅ 包不存在 → 404
  ✅ 非法条目 PUT 被拦（4xx）
  ✅ roll 缺 pool 的条目 PUT 被拦（4xx）
  ✅ 被拦的条目没落盘（仍是 6 条）
  ✅ 域列表里有 drop_pools（前端 tab 的依据）
  ✅ 词典接口含 drop_pools 域
  ✅ 分组接口含 drop_pools 域
  ✅ 包概览里 drop_pools 域条目数正确（6）

===== 结果：通过 106 / 107 =====
  ❌ 未注册的策略名 → 明确警告 + 按引擎兜底策略解析: ["策略名 'fish_x' 不在内置策略表里（内置：fixed, table, table_choice, weighted）——★ 运行期 `roll_pool` 会抛 `UnknownStrategy`（**不再静默回落 weighted**）。本预览按 weighted 展示结构，仅供看形状；要「每项按 chance 独立掷」请改用 `table`。", '引用解析需要内容侧提供 resolver —— 预览只显示结构与权重，不判断某个引用（如 mat_a）到底是什么东西、也不会替它编一个答案。', "策略名 'fish_x' 未注册 —— 内容侧可注册自己的策略；未注册时运行期会抛 `UnknownStrategy`，**不再按 weighted 兜底**。"]
