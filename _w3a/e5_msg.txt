feat(E5): 技能 7 维 —— 两个守卫 + segment_plan_fn hook + 包侧 schema（不住引擎）

★ 裁决：**schema 住包**（引擎默认域集不新增「技能」域）。引擎侧只补两样：

1. saintess_engine/_validators.py 增两个守卫（与既有 5 个同风格、同位置、同 __all__）
   - segment_of(value, label)：一段耗时的四形态（None / str=类别名 / number=绝对秒 /
     {base}=基准秒过模型），只校验**形状**（类别集在内容侧时间模型里，枚举收紧归包内 schema）
   - layer_of(value, label, *, default=None)：射程层号（整数 ≥ 1）
     ★ 补掉旧调用点 int(info.get("reach") or 3) 遇 "near" 抛裸 ValueError 的硬伤
2. config._HOOKS 加 segment_plan_fn（共 20 个 hook）
   fn(actor, action, entry) -> {"cast":…, "recover":…} | None；不配 = 不存在（连问都不问）

包侧（C:/Users/yuyu/aetheran-package）：
  新增 schemas/skill.schema.json（7 维 + func 扁平键并集 + additionalProperties:false）
  game.json / editor/domains.json 加 skills 域；引擎 package.load 实测 ok=True

验证（_w3a/e5_smoke.py，exit=0 全绿）：
  合法 7 维条目通过（校验器取 $defs[primary]，与 editor/validate.py 同口径）
  反证 B1 cast="快速" ⇒ schema 拦下；B2 range=0/"near" ⇒ 拦下 + layer_of 点名
  反证 B3 缺 recover ⇒ 必填缺失；B4 字段名写错 castt ⇒ additionalProperties:false 拦下
  segment_of 7 种非法逐条抛对异常；hook 在名单且默认 None

★ 途中抓到设计文档自己的两处矛盾（都已按"意图"修正并记录）：
  1. 设计 B1 要求「cast='快速' 被 schema 拦下」，但它给的骨架里 str 分支是裸 string
     ⇒ 任何字符串都过。真正的解法 = **把枚举放进包内 schema**（这正是"schema 住包"的意义），
        给 str 分支加 enum: [attack, skill, item, move]。该 enum 与内容侧 TIME_MODEL.cast
        的键集是同源点，已登记为待同步项。
  2. 我第一版用 $ref: "#/$defs/segment" ⇒ 实测：引擎未装 jsonschema 时走**最小校验器**，
     它不解析 $ref。改成内联 anyOf（与设计骨架一致）。

门禁：引擎 82/82 · purity + no_game_vocabulary 双绿 · wiki_refs + editor_wiki 绿
行锚点跟账：remap_wiki_refs 位移 22 处；README 35 子包 / 110 .py / 24 377 行
新增 reference/skill-dimensions.md
