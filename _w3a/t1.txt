== drop_pools 域（随机产出形状）编辑器门禁 ==

[1] 域注册与 schema
  ✅ drop_pools 域已注册
  ✅ label = 掉落池 / icon = 🎁 / kind = data
  ✅ schema 文件存在（drop_pools.schema.json）
  ✅ primary='pool' 在 schema $defs 里且 = x-primary
  ✅ $defs 至少含 pool / pool_entry / pool_roll
  ✅ schema 里零 enum（策略名/取值必须是自由串，枚举会是内容分类）
  ✅ pool.required 只有 type
  ✅ pool_entry 必填 item
  ✅ pool_entry 允许扩展字段（additionalProperties）
  ✅ pool_roll 必填 pool
  ✅ type 字段是字符串且不设枚举（内容侧可注册新策略）
  ✅ pool 有 2 个 examples
  ✅ examples 不含具体游戏词（只用 mat_a / pool_a 这类假名）
  ✅ 域 → schema 映射表里有 drop_pools（词典接口不会漏这个域）

[2] 字段词典与分组
  ✅ 词典有 drop_pools 域且非空
  ✅ schema 全部 14 个字段都有中文名
  ✅ 每条词典都有注脚（不留空壳）
  ✅ wiki 引用逐条可解析（页存在 + 词真在页里）
  ✅ 表单分组 4 组（≥3），且每组非空
  ✅ 分组无重复字段
  ✅ 全部字段一个不漏地分到组里
  ✅ 组里没有 schema 不存在的字段（防拼错）
  ✅ 分组 id 唯一且都有标签
  ✅ all_groups 覆盖 drop_pools（前端面板不空白）
  ✅ chance / cutoff 是 0~1 → 给滑杆

[3] 预览：四种内置策略的结构与权重
  ✅ weighted 预览成功
  ✅ type / strategy_uses 正确
  ✅ entries 按占比降序（最常见的在前）
  ✅ ★ share = 权重 ÷ 池内权重和（5/6 与 1/6）
  ✅ share_pct 给人看的百分数
  ✅ 带等级窗口的条目原样列出（预览不替它判断会不会进候选）
  ✅ expanded_count/unique 与引擎直算一致
  ✅ 审计通过（无断链）
  ✅ ★ 不装懂：一定有 resolver warning
  ✅ fixed 列出全部条目、保持声明序
  ✅ fixed 的 share / w 都是 null（占比不适用，不给假数）
  ✅ fixed 的 n 原样带回
  ✅ table_choice 预览成功且有 rolls 无 entries
  ✅ rolls 列出 cutoff（互斥档）
  ✅ rolls 列出 n 区间与 fallback
  ✅ 子池被识别（is_sub_pool）并递归展开
  ✅ cutoff 累计 = 1.0 → 不给「累计不足」警告
  ✅ ★ expanded_count = 引擎对子池递归展开的和（2 + 2）
  ✅ table 列出 chance 与子池展开
  ✅ table 的 cutoff 是 None（别把两种语义混起来）
  ✅ ★ 四个池的展开计数与引擎 LootTable 直算逐项一致
  ✅ 引擎审计：这批数据本身是干净的（issues 为空）

[4] 预览：坏数据 / 池不存在 → 明确原因，不假装有内容
  ✅ 坏数据被拦（没有这条池）→ ok=False
  ✅   └ 原因里出现「没有这条池」
  ✅ 坏数据被拦（不是对象）→ ok=False
  ✅   └ 原因里出现「不是对象」
  ✅ 坏数据被拦（缺少 type）→ ok=False
  ✅   └ 原因里出现「缺少 type」
  ✅ 坏数据被拦（必须是条目数组）→ ok=False
  ✅   └ 原因里出现「必须是条目数组」
  ✅ 坏数据被拦（缺 item）→ ok=False
  ✅   └ 原因里出现「缺 item」
  ✅ 坏数据被拦（权重和）→ ok=False
  ✅   └ 原因里出现「权重和」
  ✅ 坏数据被拦（缺 pool）→ ok=False
  ✅   └ 原因里出现「缺 pool」
  ✅ 坏数据被拦（必须是数组）→ ok=False
  ✅   └ 原因里出现「必须是数组」
  ✅ 整表不是对象也被拦
  ✅ 子池不存在 → 明确 warning（前缀/引用由内容侧注册，预览不猜）
  ✅ 子池不存在 → 引擎审计报「断链」（预览与引擎同判）
  ✅ 循环引用 → 报「坏数据」而不是崩（不是 500/RecursionError 外泄）
  ✅ 审计只报**本池**的问题（不把别的池的断链算到自己头上）
Traceback (most recent call last):
  File "C:\Users\yuyu\framework-engine\tests\test_editor_loot.py", line 381, in <module>
    sys.exit(main())
             ^^^^^^
  File "C:\Users\yuyu\framework-engine\tests\test_editor_loot.py", line 371, in main
    t4_preview_bad_data()
  File "C:\Users\yuyu\framework-engine\tests\test_editor_loot.py", line 261, in t4_preview_bad_data
    out = LV.build_file({"a": {"type": "fish_x", "entries": [{"item": "mat_a", "w": 1}]}}, "a")
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\yuyu\framework-engine\editor\loot_view.py", line 480, in build_file
    return build(entry, key, pools=data, vocab=vocab)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\yuyu\framework-engine\editor\loot_view.py", line 454, in build
    audit_rep = table.audit(resolvable=_make_resolvable(v))   # ← 引擎同一份审计（声明参与判定）
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\yuyu\framework-engine\saintess_engine\loot\pool.py", line 395, in audit
    spec = self.strategy_of(pool)
           ^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Users\yuyu\framework-engine\saintess_engine\loot\pool.py", line 277, in strategy_of
    raise UnknownStrategy(
saintess_engine.loot.pool.UnknownStrategy: 未知掉落策略 'fish_x'；已注册 = ('fixed', 'table', 'table_choice', 'weighted')。★ 不许静默回落 weighted —— 那会把「每项独立判定」静默变成「必掉一件」。（若本意就是「每项按 chance 独立掷」，请用 `table`。）
