feat(E1b/P2): F2_damage_full 全链 —— 修 use 步语义（必须看到前面步的输出）+ 验收

★ 修 `formula/__init__.py::_run_chain` 的 use 分支（P4 遗留 bug）：
    v = self._eval_ref(eid, st["use"], **vrs**, ctx)   ← 错
    v = self._eval_ref(eid, st["use"], **local**, ctx)  ← 对
  `local` 才带前面所有步的输出。传 `vrs` 会让"用 use 把多条串成一条全链"**不可行**：
  被引条目看不到上一步产出的 eff_def / k_def 这类中间量。
  （`expr` 步一直是传 local 的 —— 两处不一致，不是有意区分。）

包侧（aetheran-package）新增 F2_damage_full：
  chain，4 步全用 use：F1_k_def → F1_eff_def → F1_damage_reduction → F2_damage
  ★ 必须显式写 returns：V1 的推导规则是"从**最后一步**推"，而最后一步是 use 步
    （自己没有 returns）⇒ 推不出来。（F2_damage 那条链不用写，它的末步是带 returns 的 expr 步。）
  ★ 零新语法：use 步是 E1/P4 已实现并验过的形状。

验收（_w3a/e1b_p2_verify.py，exit=0 全绿）：
  ★ 主验收：全链 ≡ 手工三步（8 组同输入逐位同值）：
    Lv1 标准 66 · Lv50 标准 100 · Lv50 高防 60 · Lv100 标准 200 ·
    穿透20% 111 · 穿透20%+固定50 113 · 满乘区 263 · 穿透60%低防 192
  逐步打印 4 步（每环可单独算给玩家看）；波动 variance>0 同种子逐次同值（99/104/100/99/95）
  回归：既有 26 条声明逐条仍可求值（含 F6 聚合两档）

★ 途中登记一个 V5 盲区（写入验收脚本 §5）：
  V5 只校验**本链自己**的 expr 变量域；`use` 步引用的条目需要哪些变量，V5 **不查**。
  ⇒ 串链少给一个变量时 `expr` **静默按 0 算**（不报错）。
  实证：把变量名 `def` 错写成 `dfn` ⇒ 全链给 150（=完全不减伤），手工给 100。
  （`def` 是 Python 关键字 ⇒ 传参只能走字典键，这条本身也是踩点。）
  → 待补 V13：串链的 use 引用，合并校验变量覆盖。

门禁：82/82 · purity + no_game_vocabulary 双绿 · README → 24 735 行
