feat(E3): 数值预算门禁 gates —— 六个纯函数校验器（零 hook、零词汇）

新增 saintess_engine/gates/__init__.py（~260 行）：
  within_budget(value, *, budget, label)        单值预算内（严格大于才红）
  spread_within(rows, *, tol, label)            同组极差（装备 5% / 技能 15%）
  monotone_by(rows, *, keys, order_key, direction, label) -> (errors, warns)
                                                成长单调（内部按等级升序排序后再比）
  within_cap(value, *, cap, label)              属性 cap（cap=None ⇒ 永远通过）
  share_within(parts, *, total, cap, label)     占比上限（主属性 60 / 词条 40 / 单源 45）
  sum_within(parts, *, budget, label)           合计预算内（6 槽 = 35%）

约定：空列表 = 通过；每条报错是完整中文句且**点名到条目**（组标签+id+字段+实测+阈值+差多少）；
数值文案 %.4g、比值 %.1%；**用法错抛异常**（TypeError/ValueError），**数据错只给文案**。

★ 为什么建：旧项目没有这个能力 —— 装备 687 条靠人肉 + 十几个临时 _audit_*.py 检查，
  所以会出现「同装等一件强三倍」。本模块让配平变成"一次跑完、报全部、给退出码"的批处理。

验证（_w3a/e3_smoke.py，exit=0 全绿）：
  ① 6 个校验器各自的"通过/报红"两态 + 固定文案格式
  ② 8 条用法错逐条抛对异常类型
  ③ ★ 真数据验收：拿 W1D 的 44 件真实装备跑 ——
     逐件 PE ≤ 预算×1.05 ⇒ 违规 0；4 个多件组极差 ≤5% ⇒ 违规 0
  ④ ★ 反证（门禁有牙）：故意把「粗铁长剑」抬价 20% ⇒ 报红并点名：
     "装备[5/粗制/武器] 极差超限：max 24（粗铁长剑）/ min 19.99（粗铁法杖）= 16.7% > 5%"
  ⑤ 锚点表 Lv1–100 五项成长单调 ⇒ 0 违规

★ 冒烟抓出 2 处问题（1 处真 bug、1 处测试自己的）：
  1. 真 bug：_num 默认拒绝负数 ⇒ 但 spread_within 明确要支持"全负组"（分母改 |min|）。
     改为默认允许负数，只在语义上不该为负处传 nonneg=True
  2. 测试自己：monotone_by 的用例数据排序后反而变成单调升，检不出东西 —— 已改成真降

门禁：引擎 82/82 · purity + no_game_vocabulary 双绿 · wiki_refs + editor_wiki 绿
行锚点跟账：README 34→35 子包 / 109→110 .py / 24 054→24 310 行；新增 reference/gates.md
structure：games/orlandia 与 examples/minimal-game 一行未改
