feat(E2/P1): 面板栈 PanelStack（键级属性聚合形状 + 逐层归因）

新增 saintess_engine/panel/__init__.py（~390 行）：
- 栈级：version / base{mode: actor|value, keys[], value{}} / layers[] / emit{int_keys, round} / audit
- 层字段：id / src / group / mode(add|mul|set) / keys[]|"*" / values|ref / apply / order /
  weight / cap / floor / when / status / note
- 求值：逐键合并；base.mode 决定基础值来源（value=声明常量，actor=调用方按 keys 取值）
- 归因：ResolvedPanel.trace(key) 逐层中间量 + .shares(key) 按 group 汇总来源占比
- 接口：from_decl / resolve / trace / shares / cached_stack（(id,version) 缓存）
- config._HOOKS 加 panel_layers_fn（19 个 hook）—— 不配 = 不存在

三条有意定死的语义（设计 §2.2.3，均有理由）：
  1. mul 的键不在基础里 ⇒ 结果 0（不改成"1 则不变"，那是静默改数值）
  2. cap/floor 只作用于 add/mul，不作用于 set（set = "这层说了算"）
  3. 不提供 drop/撤销口（面板聚合是每次全量重算，层是声明不是账本）

验证（_w3a/e2_smoke.py，exit=0 全绿）：
- 求值 6 条（add 合成 / mul / cap 生效 / int_keys / mul 缺键=0 / inactive 跳过）
- set 层 + when（dict 命中 / 不命中 fail-closed）+ weight 竞争 5 条
- ref 取数接通 + 取不到报错点名 2 条
- trace / shares 归因打印
- 8 条非法声明逐条报错点名

★ 冒烟抓出我模块里 2 个真 bug（都已修）：
  1. L["cap"] 未用 .get() —— cap/floor 是可选字段，未声明时该键不存在
  2. resolve() 忽略了 base.mode —— mode=value 时该用声明里的常量基础值，
     而不是拿调用方传的 dict（传空 dict 就变成"零基础"，静默算错）

门禁：引擎 82/82 · purity + no_game_vocabulary 双绿 · wiki_refs + editor_wiki 绿
★ 途中零词汇门禁红过一次 —— 我在 config.py 注释里写了游戏名（引擎必须零游戏知识），已中性化
行锚点跟账：tools/remap_wiki_refs.py 位移 config.py 22 处；README 33→34 子包 / 108→109 .py / 23 700→24 029 行
新增 reference/panel.md
