feat(E2/P2): 面板栈接线 —— battle/stats.py 加前置分支（未装配路径一字不动）

battle/stats.py::_player_base_stats 在 `_tb` 之后、`fn = _cfg.get_hook("panel_fn")` 之前
插入 17 行前置分支：
  _fn2 = _cfg.get_hook("panel_layers_fn")
  if _fn2 is not None:   # 未装配 ⇒ 落到下面原路（一字不动）
      actor 自报 panel_stack → _fn2(sid) 取声明 → PanelStack.from_decl().resolve()
      base.mode=value ⇒ 用声明常量；mode=actor ⇒ 按 base.keys 从 actor 裸字段取
      ctx = {refs, flags, actor, battle}
      return dict(面板)   # 普通 dict，兼容下游 st.get() 读法

两态验证（_w3a/e2_wire_verify.py，exit=0 全绿）：
  态 A 不装 ⇒ get_hook is None，面板来自旧口 panel_fn（hp 1100 / atk 60 / 带旧口 src）—— 零回归路径
  态 B 装   ⇒ 面板来自 PanelStack（hp 1000 / atk 300），旧口被完全绕过（无 src 键）
  态 B′     ⇒ base.mode=actor：基础值从 actor 取（hp 777 / atk 33+500=533）
  fail-closed 3 条 ⇒ 缺 panel_stack / 栈不存在 / actor 缺声明键，逐条点名

★ 接线里抓到并修掉一个真问题：
  初版写成 `{k: actor.get(k, 0) for k in base.keys}` —— 把「actor 没这个字段」
  静默变成 0，正是项目明禁的静默兜底（零默认值口径）。
  改为只收集存在的键，缺键时由 PanelStack.resolve 抛错点名。

门禁：引擎 82/82 · purity + no_game_vocabulary 双绿 · wiki_refs + editor_wiki 绿
行锚点跟账：remap_wiki_refs 位移 stats.py 5 处；README 24 029 → 24 054 行
structure：games/orlandia 与 examples/minimal-game 一行未改
