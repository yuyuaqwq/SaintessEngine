  · "GET /api/package/orlandia/loot/audit HTTP/1.1" 422 -
  · "GET /api/package/orlandia/d/drop_pools/boss:inst_abyss_gate/preview HTTP/1.1" 200 -
  · "GET /api/package/orlandia_bare/loot/audit HTTP/1.1" 422 -
  · "GET /api/package/orlandia_bare/d/drop_pools/boss:inst_abyss_gate/preview HTTP/1.1" 200 -
  · "GET /api/package/nope/loot/audit HTTP/1.1" 404 -
== 掉落池「包内引用词汇声明」门禁 ==

[1] 真包：带包内声明 → 审计 0 断链（不声明 = 旧口径）
  ✅ 真包有词汇声明且被读到（declared=True）
  ✅ 声明里的 ref_domains 落地成包内主键集（900 条 items）
  ✅ ★ 不声明 = 旧口径（186 处，与引擎默认逐值同）
  ❌ ★ 带声明 = 0 断链 {'ok': False, 'pool_count': 596, 'entry_count': 1435, 'issue_count': 11, 'by_kind': {'error': 11}, 'issues': [{'level': 'error', 'pool': 'fish:deep_lake', 'message': "未知掉落策略 'fish'：运行期 `roll_pool` 会抛 
  ✅ 池数 / 条目数与之前一致（596 / 1435）
      （真包审计：不声明 186 处 / 79 池 → 带声明 11 处）
  ✅ ★ 单条预览：带声明 audit.ok=True；不带 = 断链
  ✅ 预览回报「本包声明了词汇」（前端可据此说明）
  ✅ 声明过的外部引用在预览里不再被说成断链
  ✅ 没声明的包照旧给「需要内容侧 resolver」warning

[2] 零知识：一条框架从没见过的假前缀也能被声明（框架不认识它的含义）
  ✅ 不声明 → 报断链（旧行为）
  ✅ 包声明了 `zzz:` → 不报（框架只转发，不解释）
  ✅ 声明文件只认通用键（不认识的值照收，未知键忽略）
  ✅ 框架侧只留通用常量名（域名/键名），不认识任何取值
  ✅ 声明表域 loot_vocab 由内容包声明且 kind=rules（框架内置集里没有它）
  ✅ 声明表落 content/rules/（kind 决定的路径，不硬编码）
  ✅ 真包声明表能被编辑器通用接口读到（get_entry）
  ✅ 声明表域**故意**无 schema（内容侧取值不该长出框架词汇表）→ 通用校验给空集
  ✅ 声明表在包的 domains 清单里（分发/概览都靠它）

[3] 坏声明 / 缺声明 → 不崩，降级成旧行为（逐值相同）
  ✅ 缺文件 → 降级成空声明且与旧行为一致
  ✅ 不是 JSON（'{'） → 降级成空声明且与旧行为一致
  ✅ 顶层是数组（[1,2]） → 降级成空声明且与旧行为一致
  ✅ 声明表里没有本域那条（只有别的域） → 降级成空声明且与旧行为一致
  ✅ 值是错的类型 → 降级成空声明且与旧行为一致
  ✅ 列表里混了非字符串 → 降级成空声明且与旧行为一致
  ✅ 只写了未知键 → 降级成空声明且与旧行为一致
  ✅ ref_domains 指向不认识的域 → 降级成空声明且与旧行为一致
  ✅ ref_domains 是字符串不是数组 → 降级成空声明且与旧行为一致
  ✅ 半份声明不崩、且不假装 0 断链

[4] HTTP 端到端：域级审计端点 + 预览端点（真包 vs 无声明临时包）
  ❌ ★ GET /api/package/orlandia/loot/audit → 200 / 0 断链 / 596 池 422 {'ok': False, 'pool_count': 596, 'entry_count': 1435, 'issue_count': 11, 'by_kind': {'error': 11}, 'issues': [{'level': 'error', 'pool': 'fish:deep_lake', 'message': "未知掉落策略 'fish'：运行期 `roll_pool` 会抛 
  ✅ ★ 预览（含 equip: 抽行的池）→ 200 且 audit.ok
  ✅ 无声明包：审计端点 → 422（就是现在那批断链）
  ✅ 无声明包：预览照旧报断链（行为与现在一致）
  ✅ 包不存在 → 404

[5] 声明只影响「引用判定」：权重/展开/池数据一律不动
  ✅ weighted 型池 的条目/抽行逐值不变（只有审计判定变）
  ✅ table 型池 的条目/抽行逐值不变（只有审计判定变）
  ✅ fixed 型池 的条目/抽行逐值不变（只有审计判定变）
  ✅ 声明不改引擎判定：`equip:` 仍不是 inline 前缀（不参与展开外列），而改走「前缀→域」**真判**
  ✅ `external_prefixes` 只影响判定、**不**混进展开外列（该池展开数不变）
  ✅ 声明过的 inline 前缀按引擎语义参与展开外列（chest:high 变多，是**有意**的）

-- 6. 前缀→域（ref_prefix_domains）：带前缀的引用也真判 --
  ✅ 声明前缀→域后：在册的解得开、不在册的报断链（一共只报 1 条）
  ✅ 裸 ref 未被牵连（没声明 ref_domains 时它照旧不判）
  ✅ 只声明 external_prefixes 时与旧版逐值一致（该前缀下一条都不报）
  ✅ 同一前缀 external + 前缀→域 并存 → 以更严的为准（仍报 1 条）
  ✅ 域名不认识 → 声明降级成「没说」（declared=False 且 0 断链）
  ✅ 长前缀优先：`equip:eq_in` 按 items 判（items 里没有 → 报）
  ✅ 幂等：normalize 过的声明再 normalize 仍保留前缀→域（不带 pkg_dir 也不丢）
  ✅ 预览不为已声明的前缀引用补「需要内容侧 resolver」这句
  ✅ 前缀→域 不改变展开数（只影响审计判定）
  ✅ 回调上挂出了「要判的内联前缀」（引擎靠它开例外）
  ✅ 内联前缀 + 前缀→域：坏值被抓住（entries 1 条 + roll 行 1 条）
  ✅ 内联前缀 + 前缀→域：好值不报（`raw_in` 在 items 表里）
  ✅ 只声明 inline 没有域映射 → 内联引用照旧不判（零影响）

===== 结果：通过 50 / 52 =====
  ❌ ★ 带声明 = 0 断链: {'ok': False, 'pool_count': 596, 'entry_count': 1435, 'issue_count': 11, 'by_kind': {'error': 11}, 'issues': [{'level': 'error', 'pool': 'fish:deep_lake', 'message': "未知掉落策略 'fish'：运行期 `roll_pool` 会抛 
  ❌ ★ GET /api/package/orlandia/loot/audit → 200 / 0 断链 / 596 池: 422 {'ok': False, 'pool_count': 596, 'entry_count': 1435, 'issue_count': 11, 'by_kind': {'error': 11}, 'issues': [{'level': 'error', 'pool': 'fish:deep_lake', 'message': "未知掉落策略 'fish'：运行期 `roll_pool` 会抛 
