# -*- coding: utf-8 -*-
"""saintess_kit —— 通用游戏运行时骨架（与 `saintess_engine` 并列）。

分工
----
| 包 | 定位 | 边界 |
|---|---|---|
| `saintess_engine` | **协议层**：事件总线 / 效果叠层 / 声明表 / CTB 调度 / 落地结算 / 存档 | 极窄且稳定 |
| `saintess_kit`    | **运行时层**：存储骨架 / 命令框架 / 事件 / 时钟 / 容器 / 会话适配 | 长期演进 |

为什么分两个包而不是塞进一个：两条**独立的兼容性边界** —— 引擎已上线且纯度门禁
（`tests/test_engine_purity.py`）盯得很紧，通用件会持续长大。合并会让「引擎稳定」
与「运行时演进」互相牵扯。

铁律（与引擎同）
----------------
1. **零游戏知识**：不得出现任何具体游戏的职业/技能/怪物/地图/物品/机制名
   （`tests/test_no_game_vocabulary.py` 守）
2. **零宿主绑定**：不得 import 具体宿主（AstrBot 等）—— 宿主差异经适配接口注入
   （`tests/test_engine_purity.py` 的包清单含本包）
3. **只用标准库**：与引擎一致，`pip install` 即用
"""
